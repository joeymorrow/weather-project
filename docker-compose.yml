services:
  weather-dashboard:
    build: .
    ports:
      - "5000:5000" # Explicit port mapping for security and isolation
    restart: always
    volumes:
      - ./data:/app/data # Mount persistent data securely
